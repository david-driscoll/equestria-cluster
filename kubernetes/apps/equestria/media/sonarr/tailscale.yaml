---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sonarr-tailscale
spec:
  defaultBackend:
    service:
      name: sonarr
      port:
        number: 6767
  ingressClassName: tailscale
  tls:
    - hosts:
        - sonarr
