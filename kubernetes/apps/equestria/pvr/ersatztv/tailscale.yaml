---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ersatztv-tailscale
spec:
  defaultBackend:
    service:
      name: ersatztv
      port:
        number: 8096
  ingressClassName: tailscale
  tls:
    - hosts:
        - ersatztv
