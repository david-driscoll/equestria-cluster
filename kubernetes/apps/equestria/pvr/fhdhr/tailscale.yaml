---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fhdhr-tailscale
spec:
  defaultBackend:
    service:
      name: fhdhr
      port:
        number: 32400
  ingressClassName: tailscale
  tls:
    - hosts:
        - fhdhr
