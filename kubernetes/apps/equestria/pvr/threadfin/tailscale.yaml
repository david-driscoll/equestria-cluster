---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: threadfin-tailscale
spec:
  defaultBackend:
    service:
      name: threadfin
      port:
        number: 8096
  ingressClassName: tailscale
  tls:
    - hosts:
        - threadfin
