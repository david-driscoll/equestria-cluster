---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.34.2/service.json
apiVersion: v1
kind: Service
metadata:
  annotations:
    tailscale.com/tailnet-fqdn: "glance-k8s-sgc.${TAILSCALE_DOMAIN}"
    tailscale.com/proxy-group: "observability"
    # tailscale.com/tags: "tag:observability"
  name: glance-k8s-sgc
spec:
  externalName: unused
  type: ExternalName
  ports:
    - port: 8080
      targetPort: 8080
      name: http
    - port: 443
      targetPort: 443
      name: https

