---
apiVersion: v1
kind: Service
metadata:
  annotations:
    tailscale.com/tailnet-fqdn: "${APP}-thanos-write.${TAILSCALE_DOMAIN}"
    tailscale.com/proxy-group: egress
  name: ${APP}-thanos-write
spec:
  externalName: unused
  type: ExternalName
  ports:
    - port: 10901
      name: grpc
    - port: 10902
      name: http
