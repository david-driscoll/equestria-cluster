---
# yaml-language-server: $schema=https://raw.githubusercontent.com/david-driscoll/stargate-command-cluster/refs/heads/main/schemas/kuma.schema.json
apiVersion: autokuma.bigboot.dev/v1
kind: KumaEntity
metadata:
  name: ${CLUSTER_CNAME}-apps
spec:
  config:
    name: ${CLUSTER_TITLE}
    type: group
    parent_name: apps

---
# yaml-language-server: $schema=https://raw.githubusercontent.com/david-driscoll/stargate-command-cluster/refs/heads/main/schemas/kuma.schema.json
apiVersion: autokuma.bigboot.dev/v1
kind: KumaEntity
metadata:
  name: ${CLUSTER_CNAME}-system
spec:
  config:
    name: ${CLUSTER_TITLE}
    type: group
    parent_name: system
