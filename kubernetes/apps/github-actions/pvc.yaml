---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nuget-cache-nfs2
  namespace: github-actions
  annotations:
    volumeType: local
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: truenas-stash
  resources:
    requests:
      storage: 100Gi

